<div>
  <% @issues.each do |issue| %>
    <p><%= link_to issue.id, issue_path(id: issue.id) %> <%= issue.title %></p>
  <% end %>
</div>

<div>
  <%= button_to "Create", {controller: :issues, action: :new,}, method: :get %>
</div>
